const moment = require("moment");

let body = document.querySelector("body");
let heading = document.createElement("h3");
heading.innerText = moment().format("MMMM Do YYYY, h:mm:ss a");
body.append(heading);